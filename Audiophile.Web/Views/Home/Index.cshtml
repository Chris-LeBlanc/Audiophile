@model List<Audiophile.Models.ProductListDto>
@using Audiophile.Models
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    @foreach (var p in Model) 
    {
        <h1>@p.name</h1>
    }

</div>
